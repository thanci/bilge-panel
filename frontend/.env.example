# Frontend geliştirme ortamı değişkenleri
# Kopyalayın: cp .env.example .env.local

# Backend API base URL — Vite proxy üzerinden (geliştirme)
VITE_API_BASE_URL=/api

# Üretim için (subdomain kullanıyorsanız):
# VITE_API_BASE_URL=https://api.bilgeyolcu.com/api
